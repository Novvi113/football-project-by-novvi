import streamlit as st
import pandas as pd

st.set_page_config(layout="wide", page_title="Data Inspector")

st.title("🛠 Инспектор Данных (Ремонт)")
st.write("Используй этот инструмент, чтобы узнать правильные названия колонок.")

# 1. Загрузка файла
uploaded_file = st.file_uploader("1. Загрузи свой CSV файл сюда", type=["csv"])

if uploaded_file is not None:
    # Читаем файл
    df = pd.read_csv(uploaded_file)
    
    st.success("✅ Файл успешно открылся!")
    
    # 2. Показываем названия колонок
    st.subheader("Твои названия колонок (Скопируй их мне!):")
    st.code(list(df.columns))
    
    # 3. Показываем первые 5 строк
    st.subheader("Пример данных:")
    st.dataframe(df.head())
    
else:
    st.info("👆 Пожалуйста, перетяни сюда свой CSV файл, чтобы мы увидели структуру.")